#include "main.h"

/**
 * _strlen - check code
 *
 * @s: input
 *
 * Return: Always 0 (Success)
 *
 */

int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != '\0'; count++)
	{
	}
	return (count);
}
